.venv/
__pycache__/
*.py[cod]

# 各アプリが実行時に生成するファイル
apps/*/captures/
apps/*/logs/
apps/*/downloads/
apps/*/work/

# 起動用ショートカット（この PC の絶対パスが入るため共有しない）
apps/*/*.lnk

# Claude Code の許可リスト（この PC 固有の絶対パスが入るため共有しない）
.claude/settings.json

# ビルド生成物（tools/build.py の出力先）
build/
