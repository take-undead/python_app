"""アプリの名前・版・説明（Tkinter に依存しない）。

版と説明の文言をここ 1 か所に置く。画面（ツールバーと「について」の小窓）と、
将来ログや exe の情報から使うときに、文言が食い違わないようにするため。

**まだ作成中なので、版は 0.x でベータと明示する。** 対象の業務アプリでの
動作確認が済んでおらず、そのまま本番を任せられる状態ではないことを、
使う人が画面を見ただけで分かるようにしておく。
"""

from __future__ import annotations

APP_NAME = "Win RPA"
VERSION = "0.9.1"
STAGE = "ベータ版"

# 実機（対象の業務アプリ）での確認が済んだら False にする。
# ツールバーの色と「について」の警告が消える
IS_BETA = True


def short_label() -> str:
    """ツールバーの右端に出す短い表示。"""
    if IS_BETA:
        return f"⚠ {STAGE} {VERSION}"
    return f"バージョン {VERSION}"


def log_line() -> str:
    """実行ログの 1 行目に出す。

    exe を対象 PC にコピーして動かすので、ログだけ見て「いつのコードか」が
    分からないと調査が始まらない。更新したつもりで古いコードが動いていた、
    という切り分けもここで付く。
    """
    if IS_BETA:
        return f"{APP_NAME} {VERSION}（{STAGE}）"
    return f"{APP_NAME} {VERSION}"


def title_line() -> str:
    """「について」の小窓に出す 1 行目。"""
    if IS_BETA:
        return f"バージョン {VERSION}（{STAGE}・作成中）"
    return f"バージョン {VERSION}"


# 「まだ完成していない」ことの中身。何に気をつければよいかまで書く
BETA_NOTICE = (
    "対象の業務アプリでの動作確認が済んでいません。そのアプリのボタンや\n"
    "入力欄が自動操作から見えるかどうかは、実際に動かすまで分かりません。\n"
    "\n"
    "いきなり通しで動かさず、手順を 1 つ足すたびに［ここまで動かす］で\n"
    "確かめながら組んでください。スケジュール実行を任せる前に、\n"
    "［動かさず確認］を必ず 1 度通すこと。"
)

SUMMARY = (
    "決まった手順の PC 作業を、人が張り付かずに終わらせるためのソフトです。\n"
    "\n"
    "Windows の業務アプリを決まった順に自動操作して CSV を出力させ、\n"
    "日単位で貯まった CSV を 1 か月ぶんの連続データとして 1 本にまとめます。\n"
    "\n"
    "作業のひとまとまりを「シナリオ」として何本でも保存でき、上の一覧から\n"
    "選んで、そのとき動かせます。毎月の自動実行はそのうちの 1 つの使い方で、\n"
    "スケジュールに載せないシナリオを、必要なときに手で流すだけでも使えます。"
)

# （見出し, 本文）。小窓に上から順に並べる
SECTIONS: tuple[tuple[str, str], ...] = (
    (
        "できること",
        "・業務アプリを起動し、ボタンを押す／文字を入力する／一覧から選ぶ\n"
        "・アプリに CSV を出力させ、「名前を付けて保存」に応じる\n"
        "・出力先フォルダを年月日の型で自動的に作り、ファイルを振り分ける\n"
        "・フォルダ内の CSV を日時の古い順に並べて 1 本にまとめる\n"
        "・作業のまとまり（シナリオ）を選んで、そのとき 1 本だけ動かす\n"
        "・毎月決まった日時に動かしたいものは、タスクスケジューラに登録する",
    ),
    (
        "使う流れ",
        "1. 対象アプリを起動しておく\n"
        "2. ［新規］でシナリオ（手順のひとまとまり）を作る\n"
        "3. ［＋ 手順を追加］で操作を選び、［要素を選ぶ...］でボタンを指し示す\n"
        "4. 1 手順ごとに［ここまで動かす］で確かめる\n"
        "5. ［保存］する。ここまでで、いつでも動かせる状態です\n"
        "6. 毎月決まった日時に任せたいときだけ［スケジュール...］で登録する",
    ),
    (
        "そのとき 1 本だけ動かす",
        "シナリオは何本でも保存できます。上の一覧から選び、\n"
        "［最初から動かす］を押せば、そのシナリオだけが最初から最後まで走ります。\n"
        "スケジュールに登録していなくてもかまいません。\n"
        "\n"
        "毎月・毎週・不定期のどれでも、作業のまとまりごとにシナリオを分けて\n"
        "おき、必要になったものを選んで動かす、という使い方ができます。\n"
        "\n"
        "この画面すら開かずに動かすこともできます。シナリオ 1 本を指定した\n"
        "ショートカットを作っておくと、ダブルクリックだけで走ります\n"
        "（画面は出ず、結果は logs フォルダに残ります）。",
    ),
    (
        "4 つの実行ボタンの違い",
        "［動かさず確認］  アプリを操作しません。対象が見つかるかだけ調べます\n"
        "［この手順だけ動かす］ 選んだ手順 1 つだけ。前の手順は動かしません\n"
        "［ここまで動かす］   最初から、選んだ手順までを動かします\n"
        "［最初から動かす］   全部を動かします（本番と同じ）\n"
        "\n"
        "下の 3 つは実際にアプリを操作します。組み立て中は上の 2 つを使い、\n"
        "通しで動くことを確かめてからスケジュールに載せてください。",
    ),
    (
        "記録するのは座標ではありません",
        "押す対象は「画面のどこか」ではなく、ボタンそのものの識別情報として\n"
        "記録します。ウィンドウの位置や大きさが変わっても壊れません。\n"
        "アプリが更新されて識別情報の一部が変わっても、名前や構造上の位置で\n"
        "探し直します（どれで見つけたかは実行ログに残ります）。",
    ),
    (
        "気をつけること",
        "・スケジュールで動かす場合、その時刻に PC がログオン状態である必要が\n"
        "　あります。画面がロックされていると、UI を操作できないため動きません。\n"
        "・ファイルを消す機能はありません。無人で動くものが誤って消すと、\n"
        "　気づくのが 1 か月後になるためです。片付けは手で行ってください。\n"
        "・実行に失敗すると、そのときの画面が logs フォルダに残ります。",
    ),
)
